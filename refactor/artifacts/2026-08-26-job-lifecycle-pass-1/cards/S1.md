# Card S1 — Move lifecycle timeout policy to the queue boundary

## Equivalence contract

- Inputs covered: all lifecycle timeout helper callers in heartbeat, progress, success, continuation, and failure paths.
- Ordering preserved: yes; every helper call remains at the same operation point.
- Tie-breaking: N/A.
- Error semantics: unchanged; the same SQL and `Error` conversions run under the same contexts.
- Error vs panic: unchanged.
- Laziness and short-circuit evaluation: unchanged.
- Floating-point and RNG/hash order: N/A.
- Integer overflow: unchanged; `PostgresTimeout` validation and constants are identical.
- Side effects: identical PostgreSQL `set_config` calls in identical order and scope.
- Side-effect cardinality: unchanged.
- Async/Future identity: helper bodies and await boundaries are unchanged.
- Type narrowing and compile-time guarantees: unchanged Rust visibility and types; aliases preserve lifecycle-local names.
- Public API/ABI/serialization: unchanged; the module and helpers remain crate-private.
- Concurrency: no new shared state and no lock-order change.
- Resource lifecycle: transaction-local settings retain their prior lifetime.
- Telemetry: contexts and emitted diagnostics are unchanged.

## Predicted and actual source-line delta

- Predicted: +8 lines for the queue module declaration and compatibility aliases.
- Actual: +8 lines (`036404d..3ef2900`).

## Verification

- PostgreSQL regression: `jobs::queue::lifecycle_timeouts::tests::owned_lifecycle_transactions_cap_and_preserve_database_timeouts` passed on PostgreSQL 18.4 (`server_version_num=180004`).
- Clippy: zero warnings with `-D warnings`.
- No new lint disables.
- Commit body records the same equivalence contract: `3ef2900`.

[workspace]
resolver = "2"

members = [
    "runledger-core",
    "runledger-postgres",
    "runledger-runtime",
    "runledger-test-support",
]

default-members = [
    "runledger-core",
    "runledger-postgres",
    "runledger-runtime",
]

[workspace.dependencies]
runledger-core = { version = "0.1.0", path = "runledger-core" }
runledger-postgres = { version = "0.1.0", path = "runledger-postgres" }
runledger-runtime = { version = "0.1.0", path = "runledger-runtime" }
runledger-test-support = { version = "0.1.0", path = "runledger-test-support" }
tokio = { version = "1", default-features = false, features = ["macros", "rt-multi-thread", "sync", "time"] }
tracing = { version = "0.1", features = ["std"] }
